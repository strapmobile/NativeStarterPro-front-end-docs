{
  "name": "native-starter-pro-front-end",
  "version": "1.0.0",
  "description": "Thanks for purchasing the Native Starter Pro with back-end.",
  "main": "index.js",
  "scripts": {
   "docs:prepare": "gitbook install",
   "docs:watch": "npm run docs:prepare && gitbook serve",
   "docs:build": "npm run docs:prepare && rm -rf _book && gitbook build"
},
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^0.11.0",
    "gitbook-cli": "^2.3.0"
  }
}
